spring:
  application:
    name: word-trainer-api
  
  data:
    mongodb:
      uri: mongodb+srv://mustafayildirim881:MikailYildirim001++@wordtrainerkids.8xfh2e5.mongodb.net/wordtrainer?retryWrites=true&w=majority
      auto-index-creation: true

server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:votre-cle-secrete-tres-longue-minimum-256-bits-pour-hs256-changez-en-production}
  expiration: 86400000
  refresh-expiration: 604800000

# Logging
logging:
  level:
    com.wordtrainer: DEBUG
    org.springframework.security: INFO

# CORS
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500}
